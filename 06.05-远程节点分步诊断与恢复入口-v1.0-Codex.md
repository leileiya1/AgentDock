# DEX-05 远程节点分步诊断与恢复入口 v1.0

> 状态：功能底座与真实 SSH 诊断已实现，待 Claude 体验完善和原生验收  
> 优先级：P1

## 1. 用户问题

旧版节点检查只返回 online/offline 和一段 SSH stderr。用户无法知道问题发生在 DNS、端口、认证、目录权限、Git、tar 还是工具链，也不知道下一步该做什么。

## 2. 已实现的诊断协议

`ExecutionNode` 新增持久化的 `diagnostics`，每一步包含：

- 稳定步骤码；
- 通过、失败、未执行；
- 是否阻断远程验证；
- 简短摘要与脱敏详情；
- 耗时和检查时间。

当前顺序：

1. DNS 解析；
2. SSH TCP 端口；
3. SSH 免交互认证；
4. 工作根目录创建、写入和清理；
5. 平台信息；
6. Git；
7. tar 归档工具；
8. Bun、Node、Cargo、Rust、Python 工具链盘点。

DNS/TCP/SSH/目录/Git/tar 是阻断项。平台和通用工具链盘点是提示项，因为具体任务可能只需要其中一种运行时。

上游步骤失败后，依赖它的后续步骤标记为 `skipped`，不会把“没有执行”伪装成新的失败。

健康检查目录使用 `.agentflow-health-<随机 UUID>`，写入探针后立即精确删除。

## 3. 已实现的桌面功能底座

- 节点卡显示在线、不可用、未检查；
- 可展开分步诊断；
- 每步显示状态、摘要、详情、耗时和恢复建议；
- 诊断失败后自动展开明细；
- 新任务下拉框把离线节点标为“不可用”并禁选，本机始终可选；
- 节点重新编辑后清空陈旧诊断，要求重新检查；
- 诊断记录持久化，重启后仍可查看。

## 4. Claude 前端完善清单

- [ ] 节点卡默认只显示总状态和首个阻塞项，分步明细按需展开；
- [ ] 用竖向步骤线或清晰层级表达检查顺序，不要堆成 8 个无关联圆点；
- [ ] 通过、失败、未执行必须同时有图标与文字；
- [ ] 非阻断提示与阻断失败明显区分；
- [ ] 首个失败步骤自动定位，但不要自动滚动到让用户迷失的位置；
- [ ] 每个失败步骤提供短恢复建议；未来加入动作按钮时复用步骤码；
- [ ] SSH 认证失败说明 AgentFlow 不保存密码/私钥，并给出终端验证命令模板；
- [ ] 工作目录失败提供“编辑节点”快捷入口；
- [ ] 工具链盘点用能力标签展示已发现工具，不把缺少无关工具标红；
- [ ] 检查中逐步出现结果或显示明确阶段，不能只有长时间旋转图标；
- [ ] 多个节点同时存在时只让正在检查的节点旋转；
- [ ] 新任务选择远程节点时显示最近检查时间；离线节点禁选且可跳到设置修复；
- [ ] 远程运行失败时任务页提供“改用本机重新验证”与“换节点”入口；
- [ ] 200% 缩放、小窗口、长 hostname 和长错误详情下不横向溢出；
- [ ] 错误详情默认折叠，摘要保持一行可读。

## 5. 测试证据

| 测试 | 结果 | 证据 |
|---|---|---|
| 不可达端口故障注入 | 通过 | DNS passed、TCP failed、后续全部 skipped |
| 配置字段与 shell 注入保护 | 通过 | 非法 host/root 被拒绝，argv 保持引用 |
| 前端步骤/恢复文案 | 通过 | 1 passed |
| 前端诊断组件 SSR | 通过 | 1 passed |
| TypeScript | 通过 | `tsc --noEmit` |
| 真实 SSH：DNS/TCP/认证 | 通过 | `sapiece@10.72.254.144:22` |
| 真实 SSH：目录写入与清理 | 通过 | `/home/sapiece/agentflow-runs` 探针创建并删除 |
| 真实 SSH：平台/Git/tar | 通过 | Linux 7.0.0-27、Git 2.53.0、GNU tar 1.35 |
| 真实 SSH：工具链盘点 | 通过 | 发现 Python 3.14.4；其他缺失不误判节点离线 |
| 原生 AgentFlow.app | 待执行 | 等 Claude 完成第 4 节后统一打包验证 |

## 6. 原生 E2E 验收

1. 设置页添加测试节点并运行检查；
2. 核对 8 个步骤、耗时与真实 SSH 输出；
3. 临时填写错误端口，确认 TCP 失败且后续未执行；
4. 恢复端口、临时填写不可写目录，确认只在目录步骤失败；
5. 恢复目录，确认节点在线且新任务可以选择；
6. 断开网络后检查，确认 DNS 或 TCP 问题可区分；
7. 重启应用，确认最近诊断仍存在；
8. 删除未被任务引用的测试节点。

## 7. 完成定义

- Claude 第 4 节全部勾选；
- 原生 E2E 全部通过；
- 不再出现只有 `offline + stderr` 的不可行动界面；
- 总清单标记“已验证”。
