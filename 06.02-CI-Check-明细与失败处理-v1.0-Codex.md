# DEX-02 CI Check 明细与失败处理 v1.0

> 状态：已实现，待真实 PR/MR 验证  
> 优先级：P0

## 1. 用户问题与风险

旧界面只显示 `CI: failed/pending/passed`。用户不知道哪一项失败、是不是必需 Check、从哪里看日志，也无法判断状态是否过期。结果是“有治理功能，但仍要切到 GitHub/GitLab 自己找答案”。

## 2. 用户流程

1. 交付卡显示所有 Check/Job，而不只显示聚合状态；
2. 每行显示名称、工作流/阶段、必需或可选、状态和耗时；
3. 失败行显示经过脱敏和截短的失败摘要；
4. 有远端地址时提供“远端日志”链接；
5. 显示最近刷新时间和成功/失败/运行中计数；
6. 用户点击“刷新 CI”后更新任务与治理缓存；
7. 无 Check 或状态未知时明确说明“不会误判为通过”。

## 3. 数据契约

新增 `CiCheckStatus`：`pending | passed | failed | cancelled | skipped | unknown`。

新增 `CiCheck`：

- `name`
- `status`
- `required`
- `workflow`
- `description`
- `failureSummary`
- `detailsUrl`
- `startedAt`
- `completedAt`

`DeliveryRecord` 新增 `ciChecks`。数据库通过新增迁移 `0015_delivery_ci_checks.sql` 保存，不修改已经发布的旧迁移。

## 4. GitHub / GitLab 规则

### GitHub

- 分别读取全部 Check 和必需 Check；
- 安全门仍只使用必需 Check；
- `gh` 返回空必需 Check 时保持 `unknown`，不允许合并；
- 对失败 Check 按 run id 读取 `--log-failed`，只提取错误相关短句；
- 存储前使用统一脱敏器并限制长度；
- Check 的 `link` 作为远端日志入口。

### GitLab

- 优先读取 MR 返回的 pipeline jobs；
- 若未包含 jobs，则按 pipeline id 通过 `glab api` 查询；
- job 的 `allow_failure=false` 视为必需；
- 映射 stage、failure reason、web URL 和时间；
- 无 job 详情时至少显示 Pipeline 聚合行，不伪造明细。

## 5. 不允许发生的行为

- 空 Check 列表显示为通过；
- 可选 Check 失败阻止本来允许的必需 Check 安全判断；
- 把完整 CI 原始日志保存进数据库或直接塞满桌面界面；
- 在失败摘要里保留 token、密钥或内部敏感值；
- 失败行没有任何下一步入口；
- 刷新失败后覆盖上一份成功快照。

## 6. 代码落点

- 契约：`crates/contracts/src/governance.rs`
- 数据迁移：`crates/persistence/migrations/0015_delivery_ci_checks.sql`
- 平台采集与摘要：`crates/orchestrator/src/delivery.rs`
- 持久化读取：`crates/orchestrator/src/governance.rs`
- 桌面组件：`apps/desktop/src/components/governance/CiChecksPanel.tsx`
- 展示规则：`apps/desktop/src/lib/governance/ci.ts`

## 7. 单项测试记录

| 测试 | 结果 | 证据 |
|---|---|---|
| GitHub/GitLab 交付解析 | 通过 | 10 passed，1 个需真实账号的 live test ignored |
| GitHub 必需/可选 Check、失败摘要、日志 URL | 通过 | 单元测试覆盖 |
| GitHub 失败日志筛选与 run id | 通过 | 单元测试覆盖 |
| GitLab job/stage/failure reason | 通过 | 单元测试覆盖 |
| 新旧数据库迁移与保护 | 通过 | persistence 7 passed |
| 前端状态计数、摘要和耗时 | 通过 | 3 passed |
| 实际 CI 列表组件 SSR | 通过 | 名称、必需/可选、失败摘要和日志链接均存在 |
| Bun 全量 / typecheck | 通过 | 105 passed，0 failed；TypeScript 通过 |
| Rust fmt / clippy | 通过 | contracts、persistence、orchestrator、desktop release 严格模式 0 warning |
| 原生空状态和刷新入口 | 通过 | 已安装版 AgentFlow.app 创建 GitHub PR 草稿任务后，治理页正确显示未知不等于通过、最近刷新时间和刷新入口 |
| 真实 GitHub PR / GitLab MR | 阻塞 | 当前仓库没有开放 PR，不能伪造线上证据 |

## 8. 原生 E2E

### 已完成

1. 创建了未启动 Provider 的 GitHub PR 模式草稿任务 `TASK-007`；
2. 治理页确认显示“尚未收到 Check；状态未知不会被当作通过”、最近刷新时间和“刷新 CI”；
3. 取消后通过精确 task id 清理，任务已从项目列表移除，未创建远端 PR，也未调用 Provider；
4. “无 PR 时刷新错误提示”仍保留为真实交付流程补验项，不能用已清理草稿伪造通过证据。

### 首个真实 PR/MR 时补验

1. 同时准备通过、失败、运行中、取消和可选 Check；
2. 刷新后逐行对照平台；
3. 验证失败摘要已脱敏且不超过界面密度；
4. 点击“远端日志”应落到对应 job；
5. 所有必需 Check 通过前不得标记为可合并。

## 9. Claude 前端验收重点

- 行级层次要明显，失败项优先但不能全屏红色；
- 默认只展示短摘要，完整日志留在远端；
- “必需/可选”和“状态”必须是两个不同维度；
- 最近刷新时间必须常驻，减少用户对陈旧状态的焦虑；
- 在窄窗口和 200% 缩放下，链接和刷新按钮不能溢出。
