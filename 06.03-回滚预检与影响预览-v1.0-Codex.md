# DEX-03 回滚预检与影响预览 v1.0

> 状态：功能底座已实现，待 Claude 体验完善与原生验收  
> 优先级：P0  
> 目标：用户在改变 Git 历史前，先知道“能不能做、会影响什么、为什么推荐这个策略”。

## 1. 用户问题

旧版确认框只有一段固定说明，无法回答：

- 当前分支和工作区是否安全；
- merge commit 后是否已经有别人的提交；
- undo 与 revert 哪个可以执行；
- 会影响多少文件、哪些文件；
- 为什么按钮被禁用；
- 弹窗打开后 Git 状态变化会不会绕过保护。

这是高风险操作，不能只依赖前端文案或执行失败后的错误提示。

## 2. 已实现的后端协议

新增只读命令 `task_rollback_preflight`，返回 `RollbackPreflight`：

- 交付方式、目标分支、当前分支；
- HEAD、merge commit、pre-merge commit；
- 工作区是否干净；
- 后续提交数量和前 10 条摘要；
- 受影响文件总数和前 50 个路径；
- undo/revert 是否可执行；
- 两种策略各自的稳定阻塞码；
- 推荐策略和稳定推荐原因；
- 预检生成时间。

阻塞码是协议字段，不把后端自然语言错误直接当 UI 文案。当前包括：

- `task_not_merged`
- `delivery_record_missing`
- `remote_delivery_unsupported`
- `target_branch_not_checked_out`
- `dirty_working_tree`
- `merge_commit_missing`
- `pre_merge_commit_missing`
- `later_commits_exist`
- `merge_not_in_head`

执行 `task_rollback` 时会再次运行完整预检，并在真正修改 Git 前再次核对分支、工作区与 HEAD，防止弹窗数据过期后仍继续执行。

远端 GitHub PR/GitLab MR 当前明确返回“不支持本地安全回滚”，不会把只改本地仓库误称为远端回滚成功。

## 3. 已实现的桌面功能底座

- 点击“撤销刚刚的本地合并”或“创建回滚提交”后请求最新预检；
- 加载、错误、可执行、不可执行四种状态；
- 显示推荐策略、分支、工作区、merge/HEAD、后续提交数；
- 显示受影响文件和会被保护的后续提交；
- 当前策略不安全时禁用“确认执行”；
- 确认时强制刷新预检，状态变化后拒绝执行；
- 弹窗使用全局 Portal，垂直居中并限制最大高度。

## 4. Claude 前端完善清单

Claude 修改时逐项勾选，不要改变后端阻塞语义。

- [ ] 两个入口先显示轻量说明：undo 会改写尚未共享的最后一次合并，revert 会新增反向提交；
- [ ] 推荐策略使用明显但克制的“推荐”标记，不能只靠颜色；
- [ ] 当前不可用的入口仍可点击查看原因，但确认按钮必须禁用；
- [ ] 受影响文件按目录层次或可折叠列表展示，默认不超过 8 行；
- [ ] 后续提交突出“这些提交会被保留”，避免用户误以为 revert 会删除它们；
- [ ] commit 使用等宽字体、8 位短 SHA，并支持复制完整 SHA；
- [ ] 阻塞项按“先处理什么”排序：工作区改动、分支、后续提交、记录缺失；
- [ ] 加载时不闪现旧的可执行按钮；
- [ ] 预检失败时只提供取消和重试，不提供强制执行；
- [ ] 远端 PR/MR 显示“平台回滚尚未实现”，隐藏具有误导性的本地执行按钮；
- [ ] 200% 缩放和小窗口下弹窗内容可滚动，底部按钮始终可达；
- [ ] 键盘 Escape、Tab 焦点陷阱、关闭后焦点恢复和 Cmd/Ctrl+Enter 均验证；
- [ ] 执行完成后治理页显示策略、原 merge commit、rollback commit 和完成时间；
- [ ] 执行失败后保留预检内容并刷新变化项，不能只弹一条 toast。

## 5. 测试证据

| 测试 | 结果 | 证据 |
|---|---|---|
| 干净且 HEAD 等于 merge commit | 通过 | undo/revert 均可执行，推荐 undo |
| 脏工作区故障注入 | 通过 | 两种策略均被禁用，执行返回 `ROLLBACK_UNSAFE`，Git 未改变 |
| 后续提交故障注入 | 通过 | undo 被禁用，revert 可用且推荐 revert |
| 后续提交保护 | 通过 | revert 后后来文件仍存在，只撤销任务文件 |
| 受影响文件统计 | 通过 | fixture 返回 1 个任务文件 |
| Rust 实际 Git 集成测试 | 通过 | `reproducible_quality_gate_local_delivery_and_both_rollbacks_work` |
| 前端协议与文案映射 | 通过 | 1 passed |
| 回滚预检弹窗 SSR | 通过 | 1 passed，危险 undo 按钮处于 disabled |
| TypeScript | 通过 | `tsc --noEmit` |
| 原生 AgentFlow.app | 待执行 | 等 Claude 完成第 4 节后统一打包验证 |

## 6. 原生 E2E 验收

1. 在隔离测试仓库创建并合并一个本地任务；
2. 打开 undo，确认推荐、文件列表、分支和 commit 正确；
3. 制造未提交文件，确认两种策略都不可执行，且仓库未改变；
4. 清理未提交文件，在 merge 后增加一个无关提交；
5. 确认 undo 显示阻塞，revert 显示推荐并列出后来提交；
6. 执行 revert，确认后来提交和文件保留，任务文件被撤销；
7. 重启应用，确认任务仍显示已回滚和 rollback commit；
8. 清理测试任务与测试仓库，不影响用户项目。

## 7. 完成定义

- 后端预检和执行保护通过；
- Claude 第 4 节全部勾选；
- 第 6 节原生 E2E 全部通过；
- 总清单状态从“待 Claude 完善”改为“已验证”。
