# AgentDock 核心功能稳定性与交互响应清单

## 一、产品核心目标

AgentDock 的核心不是“启动多个 Agent”，而是稳定完成下面这条闭环：

```text
环境检测
→ Agent 安装与认证
→ 项目导入
→ 创建结构化任务
→ 分配开发 Agent
→ 执行开发
→ 自动验证
→ 分配审查 Agent
→ 汇总审查意见
→ 退回开发 Agent 修改
→ 再次验证与审查
→ 满足通过条件
→ 用户最终确认
→ 保存结果与共享记忆
```

无论任何环节出现异常，都应该满足三个原则：

1. 不丢失已经完成的工作；
2. 用户能够看懂当前发生了什么；
3. 用户能够继续、重试、跳过或人工介入。

---

## 二、首次启动与环境初始化

### 1. 首次启动欢迎页

#### 功能目标

让完全不会使用终端的用户知道 AgentDock 能做什么，并在几分钟内完成基础环境准备。

#### 正常响应

首次启动时展示：

```text
欢迎使用 AgentDock

让不同 AI Agent 自动完成：
开发 → 审查 → 修改 → 再审查 → 验收

开始配置
```

用户点击“开始配置”后，进入环境检测页面。

#### 界面应展示

- 当前操作系统；
- 系统架构；
- AgentDock 版本；
- Git 状态；
- 可用 Agent 状态；
- 必要运行环境状态；
- 可选运行环境状态。

#### 禁止出现

- 一打开就显示终端日志；
- 直接弹出复杂报错；
- 要求用户自己配置 PATH；
- 要求用户先去阅读长篇文档。

### 2. 环境检测

#### 检测项目

至少包括：

```text
Git
Node.js 或 Bun
系统 Shell
网络状态
可用磁盘空间
项目目录权限
系统钥匙串
已安装 Agent CLI
CLI 版本
CLI 登录状态
```

#### 正常响应

```text
环境检测完成

Git                 已安装    2.52.0
Claude Code         已安装    1.x.x
Codex CLI           未安装
Gemini CLI          已安装    0.x.x
系统钥匙串           可用
项目目录权限         正常
```

每一项使用明确状态：

- 可用；
- 未安装；
- 需要登录；
- 需要更新；
- 检测失败；
- 不受支持。

#### 检测过程中

```text
正在检测 Git……
正在检测 Claude Code……
正在验证 Codex 登录状态……
```

不能只显示一个长时间不动的加载动画。

#### 检测失败响应

```text
无法确认 Git 是否已安装

AgentDock 没有在常见路径中找到 Git。
你可以重新检测，或由 AgentDock 自动安装。

[重新检测] [自动安装] [查看详情]
```

“查看详情”中可以展示技术日志，但主界面不直接暴露复杂信息。

#### 验收标准

- 检测过程不会导致应用卡死；
- 单项检测失败不会阻止其他项目检测；
- 检测结果可以刷新；
- 检测结果不能因为缓存长期错误；
- 环境变化后可以自动更新状态。

---

## 三、一键安装功能

### 3. Git 一键安装

#### 正常流程

```text
未检测到 Git

Git 用于保存代码版本和隔离不同 Agent 的修改。

[一键安装]
```

点击后：

```text
正在准备安装……
正在下载官方安装包……
正在验证安装包……
正在安装 Git……
正在验证安装结果……
```

成功后：

```text
Git 安装成功

版本：2.x.x
安装位置：……
```

#### 安装前必须展示

- 软件名称；
- 来源；
- 版本；
- 是否需要管理员权限；
- 预计会修改的系统范围。

#### 安装失败响应

```text
Git 安装未完成

原因：系统拒绝了管理员权限。

你的任务和项目没有受到影响。

[重新安装] [查看解决方法] [手动选择 Git]
```

#### 必须避免

- 安装失败却显示成功；
- 用户关闭安装窗口后一直卡在“安装中”；
- 重复安装多个版本；
- 修改 PATH 后应用无法刷新；
- 下载来源不明确。

### 4. Agent CLI 一键安装

#### 正常流程

```text
Codex CLI
状态：未安装
功能：开发、代码审查
来源：官方
[安装]
```

点击安装后：

```text
正在检查安装条件
正在安装 Codex CLI
正在验证命令
正在读取版本信息
```

成功响应：

```text
Codex CLI 已成功安装

下一步：登录 Codex
[立即登录]
```

#### Agent 已安装但版本过旧

```text
检测到 Codex CLI 版本过旧

当前版本：0.x
推荐版本：1.x

旧版本可能不支持结构化任务和会话恢复。

[更新] [暂时使用当前版本]
```

#### Agent 安装来源

应明确区分：

- AgentDock 官方维护；
- Agent 官方安装源；
- 社区适配器；
- 用户自定义 CLI。

社区适配器必须有额外提示：

```text
这是社区维护的适配器，并非由 AgentDock 官方开发。
安装前请查看其权限和源代码。
```

#### 验收标准

- 安装过程可取消；
- 取消后状态正确恢复；
- 安装成功后自动重新检测；
- 不需要用户手动重启电脑；
- 如果必须重启应用，要明确提示；
- 不保存用户的官方账号密码。

---

## 四、Agent 登录与认证

### 5. 官方账号登录

#### 正常响应

```text
Claude Code 已安装，但尚未登录。

AgentDock 将打开 Claude 官方登录流程。
AgentDock 不会读取或保存你的账号密码。

[登录]
```

登录时：

```text
正在等待 Claude Code 完成官方认证……
```

成功后：

```text
Claude Code 登录成功

账号状态：可用
模型访问：已验证
```

#### 登录超时

```text
登录尚未完成

可能是登录页面被关闭，或认证等待时间过长。

[继续等待] [重新登录] [取消]
```

#### 登录失效

运行任务前发现登录失效：

```text
Claude Code 的登录状态已失效

当前任务尚未开始，项目没有被修改。

[重新登录] [更换开发 Agent]
```

任务执行中登录失效：

```text
Claude Code 认证已失效，任务已安全暂停

已保存：
- 当前代码修改
- Agent 输出
- 执行进度
- 审查状态

重新登录后可继续执行。

[重新登录并继续]
```

### 6. API Key 配置

#### 正常响应

```text
配置 API 访问

API Key 将保存在系统安全凭据存储中，
不会写入项目文件或普通配置文件。
```

需要提供：

- 验证连接；
- 显示隐藏；
- 更新；
- 删除；
- 标记所属服务；
- 标记最后验证时间。

#### 错误响应

```text
API Key 验证失败

服务返回：认证无效或权限不足。

请确认：
- Key 是否完整
- 当前账号是否有对应模型权限
- API 配额是否可用

[重新输入] [查看详情]
```

禁止把完整 API Key 写入日志。

---

## 五、Agent 接入协议

### 7. Agent 注册与能力声明

每个接入 Agent 应返回：

```text
名称
版本
协议版本
支持的角色
支持的输入形式
支持的输出形式
是否支持结构化输出
是否支持会话恢复
是否支持流式事件
是否支持代码修改
是否支持审查
是否支持工具调用
是否支持沙箱
需要的权限
```

#### 正常响应

```text
Agent 接入成功

名称：Example Agent
协议版本：1.0
支持角色：
✓ 开发
✓ 审查
✗ 测试执行

支持能力：
✓ 结构化输出
✓ 会话恢复
✓ 流式状态
```

#### 协议版本不兼容

```text
无法接入该 Agent

Agent 使用协议版本：0.8
AgentDock 支持协议版本：1.0

该 Agent 需要更新适配器后才能使用。

[检查更新] [查看兼容说明]
```

#### 能力不足

```text
该 Agent 不能承担开发任务

它当前只声明了“审查”能力。

请选择支持代码修改的 Agent。
```

#### Agent 无响应

```text
Agent 没有在规定时间内响应

AgentDock 已停止本次连接尝试，没有启动任务。

[重新连接] [查看日志] [禁用该 Agent]
```

---

## 六、项目导入

### 8. 打开本地项目

用户选择目录后，AgentDock 检测：

- 是否为 Git 仓库；
- 当前分支；
- 是否有未提交修改；
- 是否存在冲突；
- 项目语言；
- 包管理器；
- 测试命令；
- 构建命令；
- 项目规模；
- 配置文件；
- 权限。

正常结果：

```text
项目已准备完成

项目：AgentDock
分支：main
Git 状态：干净
技术栈：Tauri / Rust / React / TypeScript
测试命令：已识别
构建命令：已识别
```

#### 不是 Git 仓库

```text
当前目录还不是 Git 仓库

AgentDock 建议初始化 Git，以便保存每一轮 Agent 修改并支持安全回退。

[初始化 Git] [只读打开] [取消]
```

#### 有未提交修改

```text
项目中存在未提交修改

为了避免 Agent 修改覆盖你的工作，请选择处理方式：

[创建安全快照]
[提交当前修改]
[继续但不隔离]
[取消]
```

推荐默认选择“创建安全快照”。

#### 存在冲突

```text
项目当前存在 Git 冲突

AgentDock 不会在冲突未解决时启动自动开发，
以避免进一步破坏项目。

[查看冲突文件] [解决后重新检测]
```

### 9. 项目自动分析

```text
正在分析项目结构……
正在识别构建命令……
正在识别测试框架……
正在读取项目约定……
```

完成后：

```text
项目分析完成

已识别：
- 主要语言
- 项目入口
- 构建命令
- 测试命令
- 代码规范
- 关键目录
```

分析不确定：

```text
AgentDock 无法确定测试命令

可能的命令：
1. bun test
2. cargo test

请选择默认测试命令，或手动输入。
```

不要擅自执行高风险命令。

---

## 七、结构化任务创建

### 10. 任务基本信息

任务至少包含：

```text
任务标题
目标
详细需求
验收条件
限制条件
允许修改范围
禁止修改范围
参考文件
开发 Agent
审查 Agent
最大返工轮数
通过规则
```

用户只输入一句话时，AgentDock 可以自动生成任务草稿并要求确认。

#### 禁止行为

- 自动生成验收条件后直接运行，不让用户查看；
- 把模糊目标当作确定需求；
- 将 Agent 自己提出的额外需求默认为必须实现；
- 未提示就允许 Agent 修改整个项目。

### 11. 验收条件

每条验收条件应支持：

- 必须满足；
- 建议满足；
- 可人工确认；
- 可自动验证；
- 由 Reviewer 判断；
- 优先级；
- 关联文件或命令。

示例：

```text
必须满足
✓ cargo test 全部通过
✓ 登录失败不能创建会话
✓ 不允许修改数据库结构

人工确认
○ 登录页面视觉效果是否符合预期
```

不合理条件提示：

```text
验收条件“代码必须完美”无法被明确验证。

建议改为：
- 所有测试通过
- 不存在阻断级审查问题
- 复杂度不高于指定阈值
```

### 12. 角色分配

审查角色可分为：

- 正确性审查；
- 安全审查；
- 测试审查；
- 架构审查；
- 可维护性审查；
- 通用审查。

重复角色提示：

```text
三个 Reviewer 当前使用了相同的通用审查提示，
可能产生大量重复意见。

建议为每个 Reviewer 分配不同职责。
```

---

## 八、任务启动前检查

### 13. Preflight 检查

点击运行前必须检查：

```text
项目状态
Git 状态
Agent 可用性
Agent 登录状态
磁盘空间
网络状态
任务配置完整性
测试命令
构建命令
审批规则
预算或次数限制
```

正常响应：

```text
任务已准备完成

开发 Agent：Claude Code
Reviewer：Codex、Gemini
最大返工轮数：3
必须通过：构建、测试、2 个 Reviewer
工作区隔离：已启用

[开始任务]
```

严重风险不允许直接忽略，例如 Git 冲突或没有目录写权限。

---

## 九、开发 Agent 执行

### 14. 启动开发 Agent

```text
正在启动开发 Agent……
正在创建隔离工作区……
正在准备任务上下文……
正在加载项目记忆……
开发 Agent 已开始工作
```

运行界面应显示：

- 当前阶段；
- 当前 Agent；
- 开始时间；
- 已运行时间；
- 当前动作；
- 修改文件数量；
- 工具调用；
- 输出摘要；
- 停止按钮；
- 暂停按钮；
- 查看详细日志。

不要只显示 `Running`，应显示具体动作，但不能泄露敏感推理内容。

### 15. Agent 长时间无输出

```text
Agent 已较长时间没有响应

你可以继续等待、请求 Agent 汇报状态，或安全停止。

[继续等待] [请求状态] [停止并保存]
```

确认无响应：

```text
Agent 进程已无响应

已保存当前修改和执行记录。

[从当前状态重试]
[更换 Agent 继续]
[恢复任务前状态]
```

### 16. Agent 退出异常

```text
开发 Agent 意外退出

退出代码：1
最后阶段：修改认证接口
已修改文件：4
当前修改已保存到安全快照。

[重新启动同一 Agent]
[更换 Agent 接手]
[查看最后输出]
[恢复任务前状态]
```

绝不能直接删除修改或重置项目。

### 17. 用户中途停止

```text
是否停止当前 Agent？

停止后将保留：
- 已修改文件
- Agent 输出
- 当前任务进度
- 可恢复会话信息

[停止并保存] [继续运行]
```

---

## 十、自动验证

### 18. 构建验证

成功：

```text
构建通过

耗时：42 秒
警告：2 条
错误：0 条
```

失败：

```text
构建失败

主要错误：
src/auth.rs:42 类型不匹配

构建结果将作为必须修复的问题退回开发 Agent。

[查看完整日志]
```

### 19. 测试验证

成功：

```text
测试通过

总计：128
通过：128
失败：0
跳过：3
```

失败时应区分：

- 修改前就失败；
- 修改后新增失败；
- 不稳定测试；
- 超时；
- 测试环境失败。

测试命令异常：

```text
测试未能启动

原因：依赖未安装。

这不是测试断言失败，而是测试环境问题。

[自动安装依赖并重试]
[交给开发 Agent 处理]
[人工处理]
```

---

## 十一、审查流程

### 20. 启动 Reviewer

Reviewer 应收到：

- 原始任务；
- 验收条件；
- 修改 Diff；
- 构建结果；
- 测试结果；
- 项目约定；
- 本轮修改说明。

### 21. Reviewer 输出格式

每条问题必须包含：

```text
问题标题
严重程度
问题类型
文件位置
证据
为什么是问题
建议修改方式
是否阻止通过
关联验收条件
```

统一严重程度：

- Blocker；
- Critical；
- High；
- Medium；
- Low；
- Suggestion。

### 22. Reviewer 审查失败

```text
Codex 审查未完成

原因：Agent 意外退出。

其他 Reviewer 的结果已保存。

[重新审查]
[更换 Reviewer]
[继续使用现有结果]
```

输出无法解析时不能当作通过。

---

## 十二、审查意见聚合

### 23. 问题去重

```text
检测到 3 条相似问题，已合并为 1 条

来源：
- Codex
- Gemini
- Claude
```

合并后保留：

- 所有来源；
- 最严重等级；
- 最完整证据；
- 不同建议；
- 是否存在分歧。

### 24. 审查冲突

```text
发现审查意见冲突

Codex：建议将函数拆分以降低复杂度。
Claude：认为当前逻辑紧密，拆分会增加状态传递。

该问题不会自动触发返工，等待：
[交给裁决 Agent]
[用户决定]
[按项目规范自动判断]
```

纯风格冲突不应阻断任务。

### 25. 无效或低质量问题

应识别：

- 没有文件证据；
- 与任务无关；
- 项目原本就存在；
- 已经修复；
- 重复意见；
- 纯偏好建议；
- 与验收条件无关。

---

## 十三、通过条件判断

### 26. 基础通过规则

支持：

```text
至少 N 个 Reviewer 通过
指定 Reviewer 必须通过
构建必须通过
测试必须通过
不能存在指定等级问题
所有必须验收条件满足
达到最低评分
必须人工确认
```

未通过时必须明确列出未满足项。

### 27. 最大返工轮数

达到上限仍未通过：

```text
任务已达到最大返工轮数

当前状态：
- 构建通过
- 测试通过
- 仍有 1 个 High 问题
- 1 个 Reviewer 未通过

AgentDock 已停止自动循环，等待你的决定。

[增加一轮]
[更换开发 Agent]
[人工修改]
[接受当前风险]
[结束任务]
```

绝不能无限循环。

---

## 十四、返工流程

### 28. 生成统一返工清单

开发 Agent 不应收到杂乱原始评论，而应收到：

```text
本轮必须修复
1. 登录失败后可能创建空会话
2. 缺少错误密码测试
3. 会话接口没有处理数据库失败

不得修改
- 用户表结构
- 公共 API 路径

已确认不需要处理
- 函数命名建议
- 原有日志格式问题
```

### 29. 开发 Agent 拒绝某条意见

允许开发 Agent 提出异议，并支持：

- 提交 Reviewer 复核；
- 用户裁决；
- 裁决 Agent 判断。

### 30. 已修复问题验证

下一轮审查必须标记：

- 已解决；
- 部分解决；
- 未解决；
- 无法验证；
- 修复引入新问题。

---

## 十五、收敛控制

### 31. 无新增有效问题

上一轮问题全部解决且没有新增阻断问题时可以触发通过。

### 32. 问题数量没有下降

```text
连续两轮审查后，阻断问题数量没有下降

可能原因：
- 开发 Agent 没有理解反馈
- Reviewer 标准不一致
- 任务需求存在冲突

[更换开发 Agent]
[调整验收条件]
[人工介入]
```

### 33. 反复修改同一处

```text
检测到同一文件区域在三轮中反复修改

这可能表示审查意见冲突或方案无法收敛。

自动返工已暂停。
```

### 34. 质量倒退

如果测试失败更多、高危问题增加或旧问题重新出现，应建议回退本轮。

---

## 十六、共享记忆

### 35. 记忆写入

不能默认把所有 Agent 输出写入长期记忆，应建议用户确认：

```text
建议保存为项目记忆

“所有数据库访问必须通过 repository 层。”

来源：安全审查第 2 轮
置信度：高

[保存] [编辑后保存] [不保存]
```

### 36. 记忆分类

至少分为：

- 用户偏好；
- 项目事实；
- 架构规则；
- 编码规范；
- 已知限制；
- 历史决策；
- 失败经验；
- 任务临时记忆；
- Agent 私有会话。

每条记忆应包含来源、范围、置信度、时间、是否锁定和过期规则。

### 37. 记忆冲突

不能让 Agent 随机选择冲突记忆，应让用户或规则决定。

### 38. 记忆注入

只注入与当前任务相关的记忆，不能把整个历史记录全部塞给 Agent。

### 39. 记忆污染防护

Agent 推测必须标记为“未经确认的推测”，用户确认后才能升级为项目事实。

---

## 十七、任务暂停与恢复

### 40. 应用关闭

```text
当前有 1 个任务正在运行

请选择：
[后台继续运行]
[暂停并保存]
[取消关闭]
```

如果不支持真正后台运行，就不能展示该选项。

### 41. 异常退出恢复

```text
检测到上次未完成的任务

任务：实现用户登录
阶段：第 2 轮开发返工
最后保存：18:42
工作区状态：完整

[继续任务]
[查看当前修改]
[结束并保留代码]
[恢复任务前状态]
```

### 42. 电脑重启恢复

必须从数据库、Git 和任务事件恢复当前阶段、轮次、Agent、工作区、未处理问题和最后事件。

---

## 十八、Git 与工作区安全

### 43. 隔离工作区

每个任务最好使用 Git worktree、独立分支或临时工作目录。

### 44. Agent 修改范围

Agent 修改受保护目录时必须拦截并请求确认。

### 45. 大规模删除

删除大量文件属于高风险操作，必须人工确认。

### 46. 回退

支持：

- 回退本轮修改；
- 回退到某次审查前；
- 回退整个任务；
- 保留部分文件。

---

## 十九、权限与安全

### 47. Agent 权限声明

每个 Agent 或 Adapter 声明：

```text
读取项目
修改项目
执行命令
访问网络
读取环境变量
访问系统目录
安装软件
调用 MCP
```

### 48. 危险命令

必须拦截：

- 删除系统目录；
- 格式化磁盘；
- 修改系统账户；
- 读取敏感凭据；
- 未授权上传代码；
- 高权限安装；
- 强制推送；
- 删除远程分支。

### 49. 密钥泄露

检测到密钥时阻止提交与分享，并允许用户移除或标记误报。

---

## 二十、网络与服务异常

### 50. 网络断开

暂停外部 Agent 调用，保存本地代码和任务状态。

### 51. 服务限流

支持自动退避重试、更换 Agent 或暂停任务，不能无限高频请求。

### 52. 配额不足

保留已完成内容，支持更换 Agent、配置其他 API 或暂停任务。

---

## 二十一、结果展示

### 53. 任务完成页面

必须一眼看懂：

```text
任务已完成

状态：等待用户批准
开发轮数：2
审查轮数：2
Reviewer：2
构建：通过
测试：128/128 通过
阻断问题：0
验收条件：6/6
修改文件：9
新增代码：312 行
删除代码：84 行
```

### 54. 每轮对比

支持查看初始代码、每轮开发、每轮审查、最终结果，并展示 Diff、测试变化和问题解决情况。

### 55. 最终人工批准

涉及创建提交、合并和推送必须再次确认。

---

## 二十二、任务历史与审计

### 56. 任务历史

每个任务保留：任务目标、验收条件、Agent、每轮状态、审查意见、测试结果、权限授权、最终 Diff 和最终结论。

### 57. 事件时间线

完整记录创建、执行、验证、审查、返工、通过和用户操作时间。

### 58. 日志导出

支持导出用户报告、JSON 事件、审查报告、测试报告、最终 Diff 和调试日志。

导出前必须清理 API Key、Token、密码、敏感环境变量和本机隐私路径。

---

## 二十三、更新与兼容性

### 59. AgentDock 更新

更新前保存所有任务状态。

### 60. CLI 更新

兼容版本不应强制升级；不兼容时应明确提示已有任务和历史记录不受影响。

### 61. 协议兼容

协议应支持：

- 主版本；
- 次版本；
- 能力协商；
- 可选字段；
- 降级运行；
- 废弃提醒；
- 兼容时间窗口。

---

## 二十四、性能与资源控制

### 62. 并发任务

资源不足时建议排队执行，并允许高级用户选择继续并发。

### 63. 资源监控

显示 CPU、内存、磁盘、Agent 进程、当前任务、网络请求和队列。

### 64. 超大项目

默认排除 node_modules、target、dist、.git 和日志目录，允许用户调整。

---

## 二十五、用户提示与错误文案规范

### 65. 每个错误必须回答四件事

```text
发生了什么
为什么发生
是否影响代码或任务
用户下一步可以做什么
```

### 66. 状态颜色

```text
绿色：完成、正常、通过
蓝色：正在进行
黄色：需要注意、等待用户
橙色：可恢复异常
红色：阻断错误或危险操作
灰色：未配置、未执行、不可用
```

### 67. 操作按钮

主按钮只能有一个，其他操作降级为次要按钮。

---

## 二十六、发布前必须通过的核心测试

### 68. 安装测试

覆盖：

- 全新 Windows；
- 全新 macOS；
- 全新 Linux；
- 没有 Git；
- 没有 Node；
- PATH 异常；
- 权限不足；
- 安装中断；
- 重复安装；
- 版本过旧；
- 网络断开；
- 下载损坏。

### 69. Agent 测试

每个官方 Agent 至少测试：

- 未安装；
- 已安装；
- 未登录；
- 登录过期；
- CLI 升级；
- CLI 输出变化；
- 无结构化输出；
- 进程卡死；
- 进程退出；
- 超时；
- 限流；
- 配额不足；
- 会话恢复；
- 用户中止。

### 70. 工作流测试

覆盖：

```text
一次开发直接通过
一次开发一次返工
多 Reviewer 全部通过
部分 Reviewer 失败
Reviewer 意见冲突
Reviewer 返回重复问题
测试失败后返工
构建失败后返工
达到最大轮数
用户中途暂停
应用崩溃恢复
电脑重启恢复
更换开发 Agent
更换 Reviewer
任务最终放弃
```

### 71. 数据安全测试

必须验证：

- 崩溃后任务不丢失；
- 回退不会影响原项目；
- API Key 不写入日志；
- Adapter 无权访问未授权目录；
- Agent 无法绕过命令拦截；
- 敏感文件不会进入共享记忆；
- 导出日志会清理隐私；
- 删除任务不会误删项目代码。

---

## 二十七、第一版必须做到极致的十项能力

1. 环境检测准确；
2. 一键安装成功率高；
3. Agent 启动稳定；
4. 项目绝对安全；
5. 任务状态不丢失；
6. 审查结果结构化；
7. 多 Reviewer 意见可管理；
8. 自动返工能够收敛；
9. 通过条件真实可靠；
10. 用户始终知道发生了什么。

---

## 二十八、第一版发布门槛

```text
✓ 全新电脑可以完成环境配置
✓ 至少两个 Agent 能稳定安装和运行
✓ 任务可以完整跑完开发—审查—返工—复审
✓ 应用关闭后任务可以恢复
✓ Agent 崩溃不会丢失代码
✓ 多 Reviewer 意见可以去重和聚合
✓ 达到最大轮数后不会继续循环
✓ 用户可以回退任意一轮
✓ API Key 和登录凭据安全保存
✓ 每一个错误都有清晰可执行的解决方式
✓ 至少完成 20 个真实项目任务测试
✓ 至少完成 100 次完整工作流压力测试
```

---

## 二十九、稳定性优先级

### P0：不完成就不能发布

```text
项目代码安全
任务持久化
Agent 进程管理
登录失效处理
工作区隔离
构建与测试验证
审查结果结构化
最大循环次数
崩溃恢复
敏感凭据保护
```

### P1：公开 Beta 前最好完成

```text
多 Reviewer 去重
冲突意见处理
每轮 Diff
共享记忆管理
Agent 更换
CLI 自动更新
任务报告
错误日志导出
资源限制
大项目优化
```

### P2：后续增强

```text
工作流市场
第三方 Adapter 商店
团队共享
云同步
SSO
企业策略
远程 Agent
组织级记忆
高级统计
自动创建 PR
```

---

## 三十、最终产品判断标准

用户不应该感觉自己在操作：

```text
CLI
进程
协议
工作区
模型上下文
Git 命令
JSON
```

用户应该感觉自己只是在做：

```text
描述任务
选择开发者
选择审查者
设置完成标准
点击开始
查看结果
决定是否接受
```

> 用户只负责定义“要完成什么”和“什么情况下算完成”，AgentDock 负责安装工具、组织 Agent、保存进度、控制风险、执行审查，并将一个可验证的最终结果交给用户。
