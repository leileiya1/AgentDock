# DEX-04 可复现复验对比与限制说明 v1.0

> 状态：功能底座已实现，待 Claude 体验完善与原生验收  
> 优先级：P0  
> 目标：不再把“同一 commit 又跑了一遍”笼统称为完全可复现。

## 1. 已解决的核心问题

旧界面只有清单哈希和一个“可复现复验”按钮，用户看不到：

- 复现可信等级；
- 原验证与复验的差异；
- 环境是否发生漂移；
- 漂移发生在哪一类依赖；
- 复验被阻止或基础设施失败后的恢复动作；
- 失败尝试是否在重启后仍可追溯。

## 2. 后端协议与持久化

新增三种复现等级：

- `fixed_commit`：固定代码提交和验证配置，不保证环境一致；
- `environment_locked`：工具、环境摘要与依赖快照必须匹配；
- `hermetic`：容器 digest 和依赖满足密闭条件，可信等级最高。

新增持久化的 `QualityReplayAttempt`：

- 状态：通过、验证未通过、环境漂移停止、基础设施失败；
- 原验证质量与最近复验质量；
- 分数变化；
- 环境是否匹配；
- 漂移类别与变化项名称；
- 脱敏后的错误码、错误详情和完成时间。

环境变量只记录并展示变量名与摘要是否变化，不返回变量值。失败或漂移尝试写入 `quality_replay_attempts`，应用重启后仍可查询。

锁定环境或密闭复现检测到漂移时，会在运行验证命令前停止；固定提交等级允许继续运行，但必须明确显示环境不匹配，不能把结果描述为环境一致。

## 3. 已实现的桌面功能底座

- 清晰展示复现等级及其可信边界；
- 原验证与最近复验并排显示；
- 显示分数变化和环境匹配状态；
- 漂移按工具、变量、系统依赖、容器、子模块、LFS、外部依赖分类；
- 每一类漂移提供具体恢复建议；
- 显示当前清单的限制；
- 原始哈希与执行环境折叠到技术详情，避免默认信息过载；
- 复验返回业务状态，不再把“环境漂移”只变成一次性 toast 错误。

## 4. Claude 前端完善清单

- [ ] 复现等级放在标题右侧，并为三种等级使用不同图形或文字，不只依赖颜色；
- [ ] `fixed_commit` 必须显式写“环境可能变化”，不能显示“完全可复现”；
- [ ] 原验证与复验卡片保持同一比较维度：分数、门禁结果、时间；
- [ ] 分数下降、通过变失败时提高视觉优先级，但不要整块大红；
- [ ] 环境漂移按类别折叠，变化项默认最多展示 5 个；
- [ ] 每种漂移显示一个最直接恢复动作；未来接入动作按钮时复用当前协议，不解析错误字符串；
- [ ] 基础设施失败与“测试断言失败”必须视觉区分；
- [ ] 没有原验证时显示“无法比较”，不能默认差值为 0；
- [ ] 没有复验时展示下一步说明，而不是空白卡片；
- [ ] 清单限制放在等级说明之后，严重限制优先；
- [ ] 哈希、工具版本、环境摘要放在折叠技术详情中并支持复制；
- [ ] 复验运行中显示不可重复点击和阶段说明；
- [ ] 复验完成后自动刷新卡片，不要求用户切换页面；
- [ ] 200% 缩放、小窗口、长依赖名和 30 条漂移项下仍可读；
- [ ] 日志和事件树只显示“复验结果摘要”，技术原始记录保留在技术日志。

## 5. 测试证据

| 测试 | 结果 | 证据 |
|---|---|---|
| 固定 revision 真实复验 | 通过 | 在 detached worktree 上运行验证并得到 100 分 |
| 环境锁定无漂移 | 通过 | 状态 `succeeded`，environment_match=true |
| 外部依赖漂移故障注入 | 通过 | 验证前停止，状态 `drift_blocked` |
| 漂移分类与键名 | 通过 | `external_dependencies` / `fixture-db` |
| 失败尝试持久化 | 通过 | 治理查询重读到最近 `drift_blocked` 尝试 |
| 环境变量保护 | 通过 | 协议只返回变化变量名，不返回值 |
| 数据库迁移与备份回归 | 通过 | persistence 7 passed |
| 前端等级/恢复文案 | 通过 | 1 passed |
| 前端对比组件 SSR | 通过 | 1 passed |
| TypeScript | 通过 | `tsc --noEmit` |
| 原生 AgentFlow.app | 待执行 | 等 Claude 完成第 4 节后统一打包验证 |

## 6. 原生 E2E 验收

1. 打开有运行清单的历史 revision；
2. 核对复现等级、限制、原验证分数；
3. 执行无漂移复验，确认新结果和差值自动出现；
4. 修改隔离 fixture 的外部依赖快照，执行环境锁定复验；
5. 确认验证命令没有运行，页面显示漂移类别、变化项和恢复建议；
6. 重启应用，确认漂移记录仍存在；
7. 恢复原快照后重试，确认状态从漂移阻止变为成功；
8. 清理 fixture，不修改用户真实项目配置。

## 7. 完成定义

- 第 4 节全部勾选；
- 第 6 节原生 E2E 全部通过；
- 不存在“固定提交=完全复现”的误导文案；
- 总清单标记为“已验证”。
