# DEX-07 项目配置信任 Diff v1.0

> 状态：功能底座已实现，待 Claude 体验完善与原生验收  
> 优先级：P1

## 1. 用户问题

旧版授权弹窗只显示验证步骤名称、额外命令和 SHA。用户看不到命令 argv、超时、环境变量、外部依赖、容器 digest，也不知道当前文件相对上次批准具体变化了什么。

## 2. 已实现的安全协议

`ProjectConfigTrust` 新增：

- 完整验证命令名称、argv 和超时；
- Agent 额外命令权限；
- 环境变量摘要 allowlist；
- 外部依赖名称；
- 容器镜像与 digest；
- 环境锁定和密闭复现开关；
- 上次批准 SHA；
- 相对上次批准的语义 Diff；
- 每项 Diff 的新增/删除/修改和高风险标记；
- 只有注释、空白、格式变化时的 `byte_only_change`。

批准时在仓库外持久化脱敏后的语义快照。外部依赖状态只保存短 SHA 摘要；命令参数经过凭据脱敏；环境变量只保存名称，不保存值。

批准请求还必须携带弹窗展示的 `expected_sha256`。如果弹窗打开后配置被 Agent 或其他进程修改，后端返回 `PROJECT_CONFIG_CHANGED_DURING_APPROVAL`，不会批准用户没有审查的新内容。

即使只是注释或空白变化，字节 SHA 变化也会使授权失效，但页面会明确说明权限语义未变化。

## 3. 已实现的桌面功能底座

- 授权前显示相对上次批准的语义 Diff；
- 权限相关变化显示高风险数量；
- 显示实际 argv 和每步超时；
- 显示额外命令、环境变量、外部依赖、容器和复现开关；
- 显示上次/当前 SHA；
- 区分“语义变化”和“仅字节变化”；
- 配置未批准或变化后，任务启动仍在任何 Git 副作用前被后端阻止。

## 4. Claude 前端完善清单

- [ ] 弹窗先显示“权限变化摘要”，再显示完整当前权限；
- [ ] 新增、删除、修改使用图标与文字，不只用红绿；
- [ ] 验证命令按步骤展示 name、argv、timeout，argv 使用等宽字体；
- [ ] shell 命令包含 `-c` 时增加高风险说明，但不擅自解析成可执行按钮；
- [ ] 环境变量只展示名称，并明确值不会被保存或显示；
- [ ] 外部依赖只展示名称和摘要变化，不暴露原状态值；
- [ ] 容器必须突出 digest；只有 tag 时提示可复现性不足；
- [ ] 注释/格式变化显示低风险说明，但仍要求重新批准 SHA；
- [ ] 首次授权显示“新增权限”，不要伪装成与空文件的普通 Diff；
- [ ] 已信任状态默认收起技术详情；
- [ ] 高风险变化默认展开并置顶；
- [ ] 超过 10 个变化项时按 section 折叠；
- [ ] 批准按钮文案保留“只批准这个 SHA-256”；
- [ ] 文件在弹窗打开后再次变化时，后端批准应拒绝或批准最新快照后 UI 立即刷新；不得继续显示旧 Diff；
- [ ] 200% 缩放、长 argv、长镜像名和 30 个变化项下按钮始终可达。

## 5. 测试证据

| 测试 | 结果 | 证据 |
|---|---|---|
| 未授权启动保护 | 通过 | 任务保持 DRAFT，无分支和工作树副作用 |
| 批准后真实验证命令 | 通过 | `/bin/sh -c` 在隔离工作树执行成功 |
| 批准后字节变化故障注入 | 通过 | SHA 立即失效 |
| 注释/格式变化分类 | 通过 | `byte_only_change=true`，语义 changes 为空 |
| 命令权限变化分类 | 通过 | `agents.extra_allowed_commands` 高风险 change |
| 凭据脱敏 | 通过 | `--token=top-secret-value` 只输出 `--token=***` |
| 预览后并发修改故障注入 | 通过 | 旧 SHA 批准被拒绝，配置保持未信任 |
| 数据库迁移与备份回归 | 通过 | persistence 7 passed |
| 前端配置 Diff SSR | 通过 | 命令、前后值、外部依赖均展示 |
| TypeScript | 通过 | `tsc --noEmit` |
| 原生 AgentFlow.app | 待执行 | 等 Claude 完成第 4 节后统一打包验证 |

## 6. 原生 E2E 验收

1. 导入带 `.agentflow/project.toml` 的隔离仓库；
2. 首次授权前核对所有开放命令和复现配置；
3. 批准并运行任务，确认命令可执行；
4. 只改注释，确认授权失效且显示“仅字节变化”；
5. 修改验证 argv、额外命令和环境 allowlist；
6. 确认三类变化逐项可见并标记风险；
7. 在命令中放入测试 token，确认界面和日志不显示原值；
8. 撤销授权，确认后续任务在 Git 副作用前被阻止。

## 7. 完成定义

- Claude 第 4 节全部勾选；
- 原生 E2E 全部通过；
- 用户批准前能看到真正会开放的命令和权限；
- 总清单标记“已验证”。
