# DEX-01 队列状态持久化与重启一致性 v1.0

> 状态：已验证  
> 优先级：P0  
> 适用版本：AgentFlow 0.1.x

## 1. 用户问题与风险

旧桌面端只把暂停/恢复命令的最后一次返回值保存在 React 内存中。关闭或刷新桌面端后，界面会默认显示为“未暂停”，即使 daemon 数据库中的任务仍然暂停。这会让用户误以为任务会继续运行。

同时存在两个关联风险：

- 不存在的队列行调整优先级也会返回成功；
- 调整只写队列行、不写任务策略，下一次重新入队会恢复旧优先级。

## 2. 完成后的用户流程

1. 打开任务详情时主动读取 daemon 的权威队列行；
2. 显示正在运行、已暂停、队列位置或具体等待原因；
3. 只有 `QUEUED` 状态允许暂停、恢复和调整优先级；
4. 操作成功后立即显示完整的新状态；
5. UI 每 5 秒安全刷新，并在任务/run 事件发生时立即刷新；
6. 关闭桌面端或重启 daemon 后再次打开，状态不依赖上次 UI 内存。

## 3. 后端契约

新增 `QueueTaskState`：

- `taskId`
- `state`: `QUEUED | RUNNING | COMPLETED | FAILED`
- `paused`
- `priority`
- `position`
- `waitingReason`
- `notBefore`
- `lastError`
- `attempts`
- `enqueuedAt`
- `updatedAt`

`waitingReason` 支持：用户暂停、全局调度暂停、非运行时段、每日预算耗尽、重试退避、并发槽已满、前面有任务。

新增 Tauri Command：`queue_task_status`。暂停、恢复、优先级修改也统一返回完整 `QueueTaskState`，不再返回残缺的 mutation DTO。

## 4. 桌面端要求

- 不从 `TaskStatus` 猜测暂停状态；
- 不在 Zustand 中保存队列权威状态；
- 队列状态加载中显示“正在读取队列…”；
- 读取失败显示可点击的重试入口；
- 状态文本使用中文短句，不展示数据库字段；
- 非排队状态禁用队列修改，避免运行中误操作；
- 状态圆点不能作为唯一信息，必须同时显示文字。

## 5. 不允许发生的行为

- 重启 UI 后把已暂停任务显示为运行中；
- 对不存在的队列项返回“修改成功”；
- 任务已被 scheduler claim 后仍显示“暂停排队”；
- 每次重新入队恢复成任务创建时的旧优先级；
- 因轮询失败清空最后一次成功状态并误导用户。

## 6. 代码落点

- 契约：`crates/contracts/src/operations.rs`
- daemon 查询/修改：`crates/daemon/src/lib.rs`
- 重启集成测试：`crates/daemon/src/tests.rs`
- Tauri Command：`apps/desktop/src-tauri/src/management_commands.rs`
- 查询 Hook：`apps/desktop/src/hooks/useQueue.ts`
- 展示组件：`apps/desktop/src/components/QueueControls.tsx`
- 展示规则：`apps/desktop/src/lib/queue.ts`
- 开发预览：`apps/desktop/src/lib/tauriDevShim.ts`

## 7. 单项测试记录

| 测试 | 结果 | 证据 |
|---|---|---|
| daemon 队列/调度测试 | 通过 | 5 passed，0 failed |
| daemon 关闭并重启后读取暂停和优先级 | 通过 | `queue_state_survives_daemon_restart_and_rejects_missing_rows` |
| 不存在队列行修改优先级 | 通过 | 返回 `task is not queued` |
| 队列中文摘要与可操作性规则 | 通过 | `src/lib/queue.test.ts` 2 passed |
| 桌面端 Bun 全量 | 通过 | 101 passed，0 failed |
| TypeScript 类型检查 | 通过 | `bun run typecheck` |
| Rust fmt/clippy | 通过 | contracts、daemon、desktop release 严格模式 0 warning |
| macOS `.app` / `.dmg` 构建 | 通过 | 两个 bundle 均生成成功，安装包通过严格签名校验 |
| 原生桌面 UI | 通过 | 暂停后显示“已暂停排队”，优先级显示“优先” |
| 关闭并重开桌面 UI | 通过 | 暂停与优先级保持不变 |
| 真实 daemon 重启 | 通过 | PID 变化后再次读取，暂停与优先级保持不变 |

## 8. 原生 E2E 验收步骤

1. 创建一个不会立即消耗 Provider 的安全排队测试任务；
2. 暂停排队，确认显示“已暂停排队”；
3. 改成“优先”，确认状态和位置更新；
4. 完全退出桌面 UI，保持 daemon 运行；
5. 重新打开 UI，确认暂停和优先级仍正确；
6. 重启 daemon，再打开任务，确认结果不变；
7. 恢复排队前取消测试任务，避免实际调用 Provider。

## 9. 完成证据与遗留问题

自动化、签名安装包、桌面 UI 重开和真实 daemon 重启全部通过。

原生测试使用了独立的 `TASK-006`，全程先暂停全局调度，因此没有启动 Provider、没有发生 API 外发。验证后该任务已取消并移入回收站，权威队列行已删除；原来的全局调度设置已恢复。
