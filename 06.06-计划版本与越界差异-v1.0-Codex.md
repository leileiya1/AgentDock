# DEX-06 计划版本与越界差异 v1.0

> 状态：功能底座已实现，待 Claude 体验完善与原生验收  
> 优先级：P1

## 1. 用户问题

旧版计划审批只展示当前计划。计划被驳回或 Agent 因越界被退回后，用户无法快速回答：

- v2 相比 v1 到底改了什么；
- 驳回意见是否被处理；
- 新增或删除了哪些允许路径；
- 风险和步骤是否变化；
- Agent 实际越界修改过哪些文件；
- 越界文件是否已经被安全重置。

## 2. 已实现的后端协议

新增只读 `task_plan_review_context`：

- 返回最多 50 个计划版本及状态、驳回原因和封印信息；
- 自动比较最近两个版本；
- 分别返回新增/删除的步骤、允许路径和风险；
- 返回摘要是否变化；
- 读取最近一次 `plan:deviation` 的真实越界文件、计划 ID 和时间。

越界保护仍由后端执行：检测到不符合 `allowed_paths` 的文件后，整个本轮工作区重置到 baseline，任务回到计划审批，事件和越界路径写入审计记录。

## 3. 已实现的桌面功能底座

- 计划审批弹窗加载版本历史；
- v1/v2 摘要并排；
- 显示上次驳回原因；
- 以增/删形式展示步骤、路径与风险变化；
- 实际越界文件单独放在高优先级提示中；
- 明确说明越界工作区已经重置；
- 首个版本显示安全空状态；
- 历史读取失败不会绕过后端计划封印验证。

## 4. Claude 前端完善清单

- [ ] 计划弹窗顶部增加版本切换器，当前待批版本默认选中；
- [ ] “版本内容”和“与上一版差异”分成两个清晰视图，避免一次堆满；
- [ ] 驳回原因紧邻差异，不要埋在历史详情底部；
- [ ] 步骤、允许路径、风险分别显示增删，不能只给一个总数；
- [ ] 允许路径使用等宽字体，新增和收窄范围要容易分辨；
- [ ] 实际越界文件与“计划新增路径”视觉区分：前者是事实，后者是提案；
- [ ] 越界提示明确写工作区已经重置，用户内容没有进入候选提交；
- [ ] 若 v2 没有解决驳回原因，提供“继续驳回”快捷聚焦；
- [ ] 计划未发生结构化变化时显示明确说明；
- [ ] 超过 8 个步骤或 20 个路径时默认折叠并显示总数；
- [ ] 批准按钮旁显示将被封印的计划版本；
- [ ] 批准期间禁用版本切换和重复提交；
- [ ] 读取历史失败时允许重试，并明确后端仍会校验当前计划；
- [ ] 200% 缩放、小窗口、长路径和长中文风险下不横向溢出；
- [ ] 键盘可切换版本、展开差异并完成审批。

## 5. 测试证据

| 测试 | 结果 | 证据 |
|---|---|---|
| 计划 v1/v2 结构化 Diff | 通过 | 新步骤、路径、风险和删除风险均准确 |
| 计划批准与封印 | 通过 | 计划 SHA 固化，数据库篡改会失败 |
| 越界文件故障注入 | 通过 | `escape.txt` 被识别并持久化 |
| 越界工作区回滚 | 通过 | 允许文件和越界文件都恢复 baseline |
| 任务状态恢复 | 通过 | revision 回退并重新等待计划审批 |
| 前端比较组件 SSR | 通过 | 驳回、路径变化、越界文件均显示 |
| TypeScript | 通过 | `tsc --noEmit` |
| 原生 AgentFlow.app | 待执行 | 等 Claude 完成第 4 节后统一打包验证 |

## 6. 原生 E2E 验收

1. 创建启用计划审批的隔离任务并得到 v1；
2. 以明确理由驳回，等待 v2；
3. 对照 v1/v2 摘要、步骤、路径、风险和驳回原因；
4. 批准一个只允许单个路径的计划；
5. 故障注入一个越界文件；
6. 确认任务回到审批、越界文件可见且工作区已重置；
7. 生成新版本覆盖合理范围后再次批准；
8. 清理隔离任务，不污染用户分支。

## 7. 完成定义

- Claude 第 4 节全部勾选；
- 原生 E2E 全部通过；
- 用户能在一次弹窗中回答“为什么重审、计划改了什么、越界了什么”；
- 总清单标记“已验证”。
