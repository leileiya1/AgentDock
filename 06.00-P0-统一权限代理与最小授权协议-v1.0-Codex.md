# PERM-01 统一权限代理与最小授权协议 v1.0

> 状态：底层 v1 已实现并通过自动化验证；桌面交互与跨平台 OS 强隔离待后续阶段  
> 优先级：P0 最高优先级  
> 负责人：Codex 负责协议、Rust、安全边界和测试；Claude 负责协议稳定后的桌面交互与体验  
> 核心目标：默认不弹出 CLI 自己的授权界面，也不向 CLI 开放整台电脑；安全动作自动执行，越界动作由 AgentFlow 统一暂停、解释、授权和审计。

## 0. 2026-07-21 实现记录

本轮已完成并有自动化证据：

- [x] 稳定权限动作、风险、请求、决定、规则、有效权限与错误码 DTO；
- [x] `0019_permission_broker.sql` 持久化请求/决定/规则，并在升级时使历史 `fullAccess=true` 失效；
- [x] argv、cwd、路径、域名和环境变量规范化，以及 operation/policy SHA-256 双封印；
- [x] once/task/project_rule 精确匹配、到期、撤销、一次性原子消费和 project identity 隔离；
- [x] `BLOCKED(permission_required)` 持久化等待、daemon 决策、同 revision 检查点恢复、拒绝后安全重规划；
- [x] 不可授权动作、Reviewer/Planner/Validator 写入、过宽规则、`..` 与符号链接逃逸 fail-closed；
- [x] Provider 协议升级到 1.2，支持 `restricted/read_only`、`effective_permissions`、权限代理能力和结构化 `permission/requested`；
- [x] 动态 sidecar 运行时请求不能超过已签名 manifest；旧 1.x Provider 不会自动升级为 `full_access`；
- [x] Provider 子进程使用环境变量 allowlist、stdin 关闭、进程租约/进程树取消；未知交互式授权提示会被停止；
- [x] 权限事件进入现有递归脱敏审计；秘密 argv 和 opaque resume token 不写入权限数据库；
- [x] Tauri/TypeScript 已生成真实的请求列表、决定、规则列表和撤销命令绑定。

仍需后续阶段完成，当前不得标记发布门禁通过：

- [ ] Claude 按第 9 节实现完整桌面弹窗、规则管理、审计筛选和原生无障碍 E2E；
- [ ] 引入按域名/端口约束的本地网络代理，将 CLI 模型流量与 Agent 工具流量物理分离；
- [ ] macOS/Linux/Windows 各自的 OS 文件系统强隔离。当前 macOS 26 的 `sandbox-exec` 探测以 abort 退出，不能把它当可靠边界；
- [ ] 为 Provider resume token 增加受保护存储后再启用原地恢复；当前安全策略是停止并从阶段检查点重跑；
- [ ] Claude Code/Codex/Gemini/Qwen 四个本机真实越界 E2E，以及桌面 `.app` 第 12.3/12.4 节逐项操作；
- [ ] 单任务、限时的高级应急权限。外层强隔离未完成前保持不可用，不能退回永久 `Yolo`。

## 1. 产品决策

以下原则作为实现约束，不再由各 Provider 自由解释：

- [ ] AgentFlow 是唯一面向用户的权限决策入口，CLI 不得直接向用户弹出授权问题；
- [ ] 默认模式是“自动执行，受限沙箱”，不是“每一步询问”，也不是“完全放权”；
- [ ] CLI 内部可以自动批准，但外层 AgentFlow 沙箱必须继续限制目录、命令、网络、环境变量和进程；
- [ ] 用户授权必须精确到操作，并提供“仅这一次”“当前任务”“当前项目规则”三种范围；
- [ ] 项目级授权只能保存精确规则，不能保存“以后全部允许”；
- [ ] `sudo`、系统设置、Keychain 扫描、工作树外大范围删除等操作属于不可授权项；
- [ ] 完全放权只保留为高级应急能力，必须是单任务、限时、显式确认，不能继续作为永久项目开关；
- [ ] 审查、规划、结果修复始终只读，不能通过任何项目设置升级为写权限；
- [ ] 所有授权、拒绝、过期、撤销、自动匹配和执行结果都写入不可混淆的审计事件；
- [ ] Provider 升级或参数变化不能绕过 AgentFlow 外层权限边界。

## 2. 当前问题

当前实现只有三档内部权限：`Normal`、`ReadOnly`、`Yolo`。动态 Provider 协议只有 `normal` 和 `full_access`，无法表达一次性的命令、路径或网络授权。

各 CLI 的行为也不一致：

- Claude Code 普通开发使用 `acceptEdits + allowedTools`，完全放权使用 `--dangerously-skip-permissions`；
- Codex 普通开发使用 `workspace-write`，完全放权使用 `danger-full-access`；
- Gemini CLI 和 Qwen Code 普通开发采用 CLI 内部自动批准，同时保留自身沙箱；
- CLI 以无交互进程运行，授权问题无法可靠地直接显示在 AgentFlow 桌面端；
- 未授权动作可能表现为退出、拒绝、输出错误或等待，当前没有统一的 `permission_required` 业务状态；
- 当前 `full_access` 是项目永久设置，会影响后续开发运行，授权范围过大。

因此，不能把“CLI 不弹窗”与“主机完全放权”当成同一件事。

## 3. 威胁模型

必须同时防御以下来源：

- [ ] 用户导入的恶意或被污染仓库；
- [ ] `AGENTS.md`、`CLAUDE.md`、README、Issue、测试数据中的提示注入；
- [ ] 依赖安装脚本、构建脚本和 Git hooks；
- [ ] Agent 错误理解任务后执行的大范围命令；
- [ ] CLI 或 Provider 升级后权限参数语义变化；
- [ ] 动态 Provider/sidecar 伪造能力或请求额外权限；
- [ ] 弹窗展示后请求内容被替换的 TOCTOU 竞态；
- [ ] UI 关闭、daemon 重启或进程崩溃后遗失授权状态；
- [ ] 一个项目的授权规则误应用到另一个项目；
- [ ] 日志、错误、审计导出泄漏 token、密码和绝对敏感路径。

需要保护的资产包括：

- 用户其他项目和个人文件；
- SSH/GPG 密钥、Keychain、云凭据和环境变量；
- Git 远端、分支、tag、release 和 CI 配置；
- 系统进程、启动项、网络代理和软件安装权限；
- AgentFlow 数据库、审批封印、审计记录和 Provider 凭据；
- 当前任务以外的工作树、远程节点和执行缓存。

## 4. 统一权限分类

新增稳定的权限动作类型，前端不能根据自然语言猜测风险：

| 类型 | 示例 | 默认策略 |
|---|---|---|
| `worktree_read` | 读取当前任务文件 | 自动允许 |
| `worktree_write` | 修改普通源码和测试 | 自动允许 |
| `worktree_delete` | 删除当前任务内文件 | 普通文件允许，范围过大则询问 |
| `control_plane_write` | 修改 `.agentflow`、CI、hooks、权限配置 | 询问 |
| `command_execute` | 运行测试、格式化、构建 | 已批准 argv 自动允许，否则询问 |
| `dependency_install` | `bun install`、`cargo add` | 询问并绑定 registry 域名 |
| `network_access` | 访问 API、下载依赖 | 默认拒绝，按域名询问 |
| `environment_read` | 读取允许的环境变量 | 仅名称白名单自动允许 |
| `secret_access` | token、Keychain、SSH key | 默认拒绝，仅受控凭据注入可用 |
| `process_control` | 启动/终止子进程 | 任务进程树内允许，树外拒绝 |
| `git_read` | status、diff、log、show | 自动允许 |
| `git_mutation` | commit、reset、clean、push、tag | 交给 Orchestrator，CLI 默认拒绝 |
| `system_change` | sudo、系统设置、启动项 | 不可授权 |
| `external_path` | 访问工作树外路径 | 默认拒绝，精确路径询问 |

### 4.1 控制面文件

至少将以下路径识别为高风险控制面：

- `.agentflow/**`；
- `.github/workflows/**`、`.gitlab-ci.yml`；
- `.git/hooks/**`、`.husky/**`；
- `AGENTS.md`、`CLAUDE.md` 和 Provider 规则文件；
- `package.json` 中 scripts、依赖和 postinstall 变化；
- 权限、认证、部署、容器和基础设施配置；
- AgentFlow Provider manifest、sidecar 和插件配置。

允许 Agent 修改这些文件不代表立即信任其内容。项目配置仍必须走独立的 SHA 信任审批，Git 提交保护和风险审查继续生效。

## 5. 协议数据结构

协议版本升级时新增以下语义，具体 Rust 类型名允许调整，但字段含义不得丢失。

### 5.1 `PermissionRequest`

- `id`：不可复用的请求 ID；
- `task_id`、`revision`、`run_id`、`provider_id`、`role`；
- `action_type`：第 4 节稳定枚举；
- `summary`：后端生成的短说明，不接受 Provider 自报文案直接作为主标题；
- `reason`：Provider 原因的脱敏摘要；
- `argv`：命令必须是参数数组，不只保存 shell 字符串；
- `cwd`：必须解析后的绝对目录；
- `paths`：规范化路径和读/写/删除意图；
- `network_domains`：规范化域名、端口和协议；
- `environment_names`：只有变量名，不包含值；
- `risk_level`：`low`、`medium`、`high`、`forbidden`；
- `grantable`：不可授权动作必须为 `false`；
- `operation_sha256`：对规范化操作内容计算摘要；
- `policy_sha256`：产生请求时有效策略摘要；
- `requested_at`、`expires_at`；
- `provider_resume_token`：可选且不作为审计事实来源。

### 5.2 `PermissionDecision`

- `request_id`、`operation_sha256`、`policy_sha256`；
- `decision`：`approve`、`deny`、`cancel_task`；
- `scope`：`once`、`task`、`project_rule`；
- `expires_at`：一次和任务授权必须有有效期；
- `approved_by`：当前固定为 `human`；
- `guidance`：拒绝后可选的替代说明；
- `created_at`。

### 5.3 `PermissionRule`

项目规则必须是可解释的精确能力，不得保存任意 Provider 文案：

- Provider/角色限制；
- 动作类型；
- argv 可执行文件和参数模板；
- 允许的 cwd 和相对路径 glob；
- 精确网络域名和端口；
- 允许的环境变量名称；
- 创建人、创建时间、到期时间；
- 当前项目 identity 和规则 SHA；
- 启用、撤销和最近命中时间。

以下规则形式禁止持久化：`*` 命令、`/**` 路径、任意域名、任意环境变量、所有 Provider、永久完全放权。

### 5.4 Provider 协议

动态 Provider 协议至少新增：

- `permission_broker` 能力声明；
- `permission/requested` 通知或等价的结构化阻塞结果；
- `effective_permissions`，由 AgentFlow 计算后传入；
- `sandbox_guarantee`，声明 Provider 自身能否执行只读/工作树写入；
- `resume_after_permission` 能力；
- 稳定的 `permission_denied`、`permission_required`、`permission_expired` 错误码。

旧 Provider 只支持 `normal/full_access` 时：

- `normal` 只能在 AgentFlow 外层沙箱内运行；
- 不允许因为 Provider 不支持细粒度协议而自动升级为 `full_access`；
- 无法表达的越界请求按拒绝处理，并给用户可理解的恢复建议；
- conformance 测试必须验证 Provider 不能修改传入权限或越过 manifest。

## 6. 运行状态机

统一流程：

```text
RUNNING
  ├─ 安全动作/规则命中 ───────────────→ RUNNING
  ├─ 可授权越界动作 ─→ PERMISSION_REQUIRED ─→ WAITING_FOR_HUMAN_PERMISSION
  │                                             ├─ 允许 → QUEUED_FOR_RESUME → RUNNING
  │                                             ├─ 拒绝 → REPLAN_OR_GUIDANCE
  │                                             └─ 取消 → CANCELLED
  └─ 不可授权动作 ───────────────────→ PERMISSION_DENIED → REPLAN_OR_FAILED
```

实现要求：

- [ ] 不让 CLI 子进程无限期占用 stdin 等待用户；
- [ ] Provider 可安全暂停时保存 opaque resume token，否则停止并从阶段检查点重新运行；
- [ ] daemon 持久化请求后才能对外显示“等待授权”；
- [ ] UI 关闭、daemon 重启后请求和决定仍存在；
- [ ] 同一 `operation_sha256` 的重复请求可以折叠展示，但每次执行仍保留审计；
- [ ] 批准前重新计算任务 revision、策略 SHA、cwd、路径和操作摘要；
- [ ] 过期、任务已变化或策略变化时拒绝旧弹窗决定；
- [ ] 拒绝不等于任务失败，优先让 Agent 根据 guidance 换安全方案；
- [ ] 超过最大授权请求次数时暂停任务并提示用户，不允许无限循环骚扰。

## 7. 外层执行边界

CLI 自带沙箱只是第二道防线，AgentFlow 必须实现自己的执行边界：

- [ ] 默认可写路径只有当前任务工作树和当前 run 目录；
- [ ] AgentFlow 数据目录、主仓库、其他任务工作树默认不可写；
- [ ] 只向进程传递最小环境变量，denylist 之外还要有基础 allowlist；
- [ ] Provider API key 由受控传输层使用，不作为普通环境变量暴露给 Agent shell；
- [ ] 默认禁止 Agent 工具网络，Provider 模型调用与 Agent 命令网络必须分开审计；
- [ ] 网络授权经过本地代理或执行节点策略按域名/端口约束；
- [ ] 所有子进程进入可追踪的进程租约，取消任务时终止整个任务进程树；
- [ ] 工作树根目录删除、符号链接逃逸和 `..` 路径必须在执行前拒绝；
- [ ] 远程节点执行相同协议，不能把 SSH 节点当成天然完全可信；
- [ ] macOS、Linux、Windows 分别有真实边界测试，不用 prompt 约束冒充 OS 约束。

## 8. 内置 CLI 适配清单

### 8.1 Claude Code

- [ ] 使用无交互模式，禁止 CLI 自己等待终端授权；
- [ ] 普通开发使用安全 permission mode 和精确 `allowedTools`；
- [ ] 未允许工具转换为 `PermissionRequest`，不能直接追加全局 allowedTools；
- [ ] `--dangerously-skip-permissions` 只能在外层强隔离且单任务临时授权下使用；
- [ ] Reviewer、Planner、Repair 强制禁用 Write/Edit；
- [ ] CLI 升级后重新运行 help/capability 探测和权限回归测试。

### 8.2 Codex

- [ ] 普通开发保持 `workspace-write`，Reviewer/Planner 保持 `read-only`；
- [ ] 默认继续禁用用户配置、插件、Apps、Memory 和未审查规则；
- [ ] 沙箱拒绝事件转换为统一权限请求或明确失败；
- [ ] `danger-full-access` 不再由永久项目设置直接触发；
- [ ] `--add-dir` 只允许由精确外部路径授权生成；
- [ ] 网络和文件扩权分别授权，不能用一个按钮同时开放。

### 8.3 Gemini CLI 与 Qwen Code

- [ ] CLI 内部自动批准只在外层沙箱存在时使用；
- [ ] 普通模式不得因为 `yolo` 文案被 UI 错误显示为主机完全放权；
- [ ] Reviewer/Planner 保持 plan/read-only；
- [ ] 去除沙箱只能由单任务临时完全放权触发；
- [ ] 未知版本或缺失沙箱参数时 Provider 标记不兼容，而不是降级到无沙箱运行。

### 8.4 API 与动态 Provider

- [ ] 区分模型 API 数据外发与 Agent 工具网络；
- [ ] API Provider 不因远端模型调用而自动获得本地 shell 网络；
- [ ] manifest 只声明最大能力，实际运行权限由 AgentFlow 收窄；
- [ ] sidecar 不支持权限代理时使用兼容受限模式；
- [ ] sidecar 请求超过 manifest 的命令、域名或路径时立即隔离并记录。

## 9. 桌面端交互清单（Claude）

Claude 必须等待 Rust 契约和生成绑定稳定后再实现本节，不得在前端自行模拟授权成功。

- [ ] 设置页将“完全放权模式”改为“执行权限”，默认显示“自动执行，受限沙箱”；
- [ ] 删除永久项目级“关闭所有沙箱”开关，旧设置升级后必须失效并要求重新确认；
- [ ] 项目页显示当前权限模式、有效项目规则数和最近一次越界请求；
- [ ] 任务发生越界时，在任务树显示“等待你授权”，不能显示成 Provider 失败；
- [ ] 授权弹窗只展示用户能判断的内容：谁、想做什么、在哪执行、会访问什么、为什么需要；
- [ ] 命令使用等宽 argv 展示，同时提供简短人类说明；
- [ ] 路径显示项目相对路径，工作树外路径必须显示完整位置和高风险标记；
- [ ] 网络请求显示精确域名、端口、协议和用途；
- [ ] 明确区分“模型 API 外发”和“Agent 命令访问网络”；
- [ ] 按钮提供“允许一次”“本任务允许”“保存项目规则”“拒绝并补充指引”“取消任务”；
- [ ] 高风险请求默认只提供一次/任务授权，不提供项目规则；
- [ ] 不可授权请求隐藏允许按钮，解释为什么以及安全替代方案；
- [ ] “保存项目规则”必须二次展示实际规则，不能只显示一句“以后允许”；
- [ ] 多个请求只能在操作摘要和权限完全相同时合并，不能批量批准不同命令；
- [ ] 弹窗打开后内容变化时立即作废并重新加载，旧批准按钮不可继续使用；
- [ ] 用户拒绝后显示 Agent 是否正在换方案、等待新指引还是任务已停止；
- [ ] UI 关闭重开后继续显示未处理请求和剩余有效期；
- [ ] 项目设置提供规则列表、最近命中、到期时间、停用和撤销；
- [ ] 撤销规则不杀死已完成动作，但必须阻止下一次执行；
- [ ] 审计页能够按任务、Provider、动作类型、决定和规则筛选；
- [ ] 默认日志只显示“请求/允许/拒绝”的简短文字，技术 JSON 放高级详情；
- [ ] 不使用只有红绿点的表达，风险、状态和决定必须有文字及图标；
- [ ] 200% 缩放、小窗口、超长 argv、长路径、中文域名下按钮始终可达；
- [ ] 键盘、焦点陷阱、读屏名称、reduced-motion 和色弱模式全部验证。

## 10. 审计与隐私

每个权限事件至少记录：

- 请求 ID、任务、revision、run、Provider 和角色；
- 规范化动作摘要和 `operation_sha256`；
- 风险、是否可授权、匹配的规则 ID；
- 自动允许、人工允许、拒绝、过期、撤销或执行失败；
- 授权范围、到期时间和批准人；
- 执行前后策略 SHA；
- 实际执行结果和完成时间。

禁止记录：

- secret、token、密码和授权头原值；
- 完整敏感环境变量值；
- Keychain 内容；
- Provider 原始请求中未经脱敏的大段文本；
- 作为 shell 字符串拼接的用户输入。

审计导出继续应用递归秘密脱敏，并保留权限请求和决定之间的可验证关联。

## 11. 故障处理

- [ ] CLI 输出未知授权提示：限时停止，保存脱敏诊断，不能无限等待；
- [ ] Provider 在请求权限后崩溃：授权仍可处理，恢复时重新验证并从检查点运行；
- [ ] daemon 在等待期间重启：请求状态恢复，不重复创建规则；
- [ ] UI 批准同时任务 revision 改变：返回 `PERMISSION_REQUEST_STALE`；
- [ ] 规则在执行前被撤销：动作不得执行；
- [ ] 授权到期：停止在动作之前，不允许执行后才报告过期；
- [ ] 路径经符号链接逃出工作树：返回 `PERMISSION_PATH_ESCAPE`；
- [ ] 域名解析后地址改变或重定向到未授权域名：阻断并创建新请求；
- [ ] argv 与弹窗展示内容不同：摘要校验失败并隔离 Provider；
- [ ] Provider 请求不可授权系统能力：直接拒绝并让 Agent 换方案；
- [ ] 用户长期不处理：任务安全暂停、释放 Provider 并保留恢复入口；
- [ ] 相同请求循环出现：达到阈值后停止自动重试并提醒用户。

## 12. 自动化测试清单

### 12.1 Rust 单元与协议测试

- [ ] 动作规范化和 `operation_sha256` 稳定；
- [ ] argv 数组、路径、域名和环境变量规则匹配；
- [ ] 通配符过宽规则拒绝；
- [ ] 一次、任务、项目规则的范围和到期；
- [ ] Reviewer/Planner/Repair 永远不能升级写权限；
- [ ] 旧 Provider 的受限兼容与 fail-closed；
- [ ] Provider manifest 越界请求隔离；
- [ ] 协议序列化、版本协商和 conformance fixture。

### 12.2 集成与故障注入

- [ ] 普通源码编辑不产生授权弹窗；
- [ ] 已批准测试命令自动执行；
- [ ] 未批准安装命令进入 `PERMISSION_REQUIRED`；
- [ ] 批准一次后仅执行一次；
- [ ] 任务规则不能跨 task；
- [ ] 项目规则不能跨 project identity；
- [ ] 修改 argv、cwd、路径、revision 或策略后旧批准失效；
- [ ] 工作树外读写、符号链接逃逸和路径穿越被阻断；
- [ ] 未批准网络、重定向和 DNS 变化被阻断；
- [ ] sudo、Keychain 扫描和系统设置始终拒绝；
- [ ] UI/daemon/Provider 分别崩溃后状态可恢复；
- [ ] 取消任务终止完整进程树；
- [ ] 日志、数据库和审计导出不含测试 secret；
- [ ] 旧 `full_access=true` 升级后不会继续主机完全放权。

### 12.3 内置 CLI 真实 E2E

对 Claude Code、Codex、Gemini CLI、Qwen Code 分别执行：

1. 在隔离仓库修改普通源码并运行白名单测试，全程不询问；
2. 请求工作树外文件，确认 AgentFlow 弹窗且文件未读取；
3. 请求安装依赖和访问测试域名，确认命令、路径、域名准确；
4. 选择“允许一次”，确认第二次请求仍需授权；
5. 选择“本任务允许”，确认其他任务不能复用；
6. 保存精确项目规则，确认只匹配同一 argv 和域名；
7. 修改弹窗打开后的请求内容，确认批准失败；
8. 拒绝并补充指引，确认 Agent 改用安全方案；
9. 锁屏、关闭 UI、重启 daemon 后恢复等待状态；
10. 搜索日志、数据库和导出文件，确认测试 secret 不存在。

### 12.4 桌面端原生 E2E

- [ ] 授权弹窗使用 body portal 并垂直居中；
- [ ] 底部操作右对齐，小窗口不拥挤；
- [ ] 用户可在 10 秒内看懂请求范围和风险；
- [ ] 禁止项没有任何可误触的允许按钮；
- [ ] 授权完成后任务树立即继续且状态正确；
- [ ] 拒绝、过期、撤销和过期弹窗都有明确恢复动作；
- [ ] 规则管理与审计筛选可用；
- [ ] 200% 缩放、键盘和读屏验收通过。

## 13. 分阶段实现顺序

必须一个阶段测试通过后再进入下一个阶段。

### 阶段 A：协议与持久化

- [ ] 新增权限 DTO、稳定错误码和状态；
- [ ] 数据库 migration、请求/决定/规则表；
- [ ] operation/policy seal 和 TOCTOU 校验；
- [ ] Provider 协议升级与 conformance 测试；
- [ ] 当前 `Normal/ReadOnly/Yolo` 兼容层。

### 阶段 B：Broker 与执行边界

- [ ] 默认策略矩阵；
- [ ] 路径、argv、网络、环境和进程约束；
- [ ] daemon 持久化等待与恢复；
- [ ] 一次/任务/项目规则匹配；
- [ ] 审计和递归脱敏；
- [ ] 故障注入通过。

### 阶段 C：内置 Provider 适配

- [ ] Claude Code；
- [ ] Codex；
- [ ] Gemini CLI；
- [ ] Qwen Code；
- [ ] API Provider 与动态 sidecar；
- [ ] 每个 Provider 单独真实 E2E。

### 阶段 D：桌面端

- [ ] Claude 按第 9 节实现；
- [ ] 前端单元/组件/无障碍测试；
- [ ] 原生 `.app` 逐项操作；
- [ ] 错误、过期、崩溃恢复和大数据量验收。

### 阶段 E：旧权限迁移

- [ ] 升级时使旧项目级 `full_access` 失效；
- [ ] 不把旧值静默转换成永久项目规则；
- [ ] 首次打开解释新的受限自动执行模式；
- [ ] 用户必须重新确认任何临时完全放权；
- [ ] 删除不再使用的危险映射和误导性文案。

## 14. 发布门禁

以下条件全部满足才能标记“已验证”：

- [ ] 默认开发任务无需 CLI 自己弹窗即可完成普通编码、测试和提交前检查；
- [ ] 默认模式无法读写工作树外文件、访问未授权网络或读取秘密；
- [ ] 任何 Provider 都不能请求后自动获得更高权限；
- [ ] 用户看到的请求与实际执行的 operation SHA 完全一致；
- [ ] 一次、任务、项目规则的隔离与到期测试全部通过；
- [ ] UI/daemon/Provider 崩溃恢复无重复执行和无授权丢失；
- [ ] 内置 CLI 真实 E2E 全部通过；
- [ ] 动态 Provider conformance 和恶意 sidecar 测试通过；
- [ ] Rust workspace release 测试和严格 Clippy 零失败；
- [ ] 前端全量测试、TypeScript、生产构建零失败；
- [ ] macOS 原生应用完整实测；
- [ ] Windows/Linux 边界至少有对应自动化证据；
- [ ] 文档、日志和审计导出均不泄漏测试凭据；
- [ ] 旧永久完全放权模式不再存在。

## 15. 完成定义

用户在日常使用中应获得以下最终体验：

1. 普通任务自动执行，不被频繁授权打断；
2. CLI 永远不直接弹出用户无法操作的终端确认；
3. 越界时 AgentFlow 用短句说明实际操作和风险；
4. 用户可以只放开完成当前任务所需的最小能力；
5. 拒绝后 Agent 会尝试安全替代方案，而不是直接失去全部进度；
6. 即使 Agent、仓库或 Provider 恶意，外层边界仍保护其他项目、秘密和系统；
7. 每次请求、决定、执行和撤销都可以复核与导出；
8. 不再需要为了避免弹窗而长期开启主机完全权限。
