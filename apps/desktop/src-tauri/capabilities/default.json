{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Permissions required by the AgentFlow main desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
