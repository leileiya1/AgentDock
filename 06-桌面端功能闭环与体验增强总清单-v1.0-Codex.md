# AgentFlow 桌面端功能闭环与体验增强总清单 v1.0

> 负责人协作方式：Codex 负责需求拆分、后端/契约复核、测试与最终验收；Claude 可按每个独立功能包文档实现桌面界面；每个功能包必须单独测试通过后才能进入下一个。

## 1. 为什么建立这份清单

目前核心后端能力已经较完整，生成契约中的主要 Tauri Command 都已有前端调用，但“有调用入口”不等于“用户能完整理解、处理失败并确认结果”。后续不再依靠聊天里零散传话，而以本文和每个功能包文档形成闭环：

1. 需求写清楚；
2. Claude/Codex 按文档实现；
3. 单项自动化测试；
4. 原生桌面端实测；
5. 记录截图、结果和遗留问题；
6. 总清单从“待做”更新为“已验证”。

## 2. 状态定义

- `待做`：只有需求，尚未编码。
- `进行中`：正在编码或修复测试。
- `已实现`：代码完成，单项测试通过。
- `已验证`：整体回归和原生桌面实测均通过。
- `阻塞`：依赖真实账号、远程服务或用户授权，必须写清阻塞原因。

不得把“存在组件”“命令可以调用”“单元测试通过”直接写成“已验证”。

## 3. 功能包总览

| ID | 优先级 | 功能包 | 当前状态 | 完成定义 |
|---|---|---|---|---|
| PERM-01 | P0 | 统一权限代理与最小授权协议 | 底层 v1 已实现并自动验证；桌面交互/跨平台强隔离待办 | 默认受限自动执行；越界操作由 AgentFlow 精确授权、恢复和审计，不再永久完全放权 |
| DEX-01 | P0 | 队列状态持久化与重启一致性 | 已验证 | 重启应用/daemon 后仍准确显示暂停、优先级、队列位置和等待原因 |
| DEX-02 | P0 | CI Check 明细与失败处理 | 已实现，待真实 PR 验证 | 展示每个 Check、失败摘要、远端日志和最近刷新时间 |
| DEX-03 | P0 | 回滚预检与影响预览 | 功能底座已实现，待 Claude 完善 | 执行前说明能否 undo、影响 commit/文件、后续提交和推荐策略 |
| DEX-04 | P0 | 可复现复验对比与限制说明 | 功能底座已实现，待 Claude 完善 | 明确复现等级、环境限制、原验证/复验差异和失败恢复动作 |
| DEX-05 | P1 | 远程节点分步诊断与恢复入口 | 功能底座已实现，待 Claude 完善 | DNS/SSH/目录/Git/工具/验证分步可见，失败可换本机或节点 |
| DEX-06 | P1 | 计划版本与越界差异 | 功能底座已实现，待 Claude 完善 | v1/v2 计划、允许路径和越界文件能对比后再批准 |
| DEX-07 | P1 | 项目配置信任 Diff | 功能底座已实现，待 Claude 完善 | 信任前看到 project.toml 的具体变化和将放开的命令/环境权限 |
| DEX-08 | P1 | 审计事件导出入口 | 功能底座已实现，待 Claude 完善 | 项目页/设置页可导出经过脱敏的审计事件并显示保存位置 |
| DEX-09 | P1 | 存储清理与备份恢复向导 | 待做 | 清理前预览、恢复分步进度、成功后可直接重启应用 |
| DEX-10 | P1 | 修复中心影响对比 | 待做 | 三种修复动作显示影响、推荐项、patch 位置和执行结果 |
| DEX-11 | P1 | Provider 与外发解释增强 | 待做 | 任务选择器说明能力、执行位置、外发对象/范围和安装修复动作 |
| DEX-12 | P2 | 列表搜索、筛选与后台运行摘要 | 待做 | 项目列表可找任务，并显示活跃/排队/等待原因和 daemon 健康 |
| DEX-13 | P2 | 大数据量、缩放与无障碍正式验收 | 待做 | 10k 日志、200 文件 Diff、200% 缩放、键盘、色弱、reduced-motion 通过 |
| DEX-14 | P2 | Windows/Linux 桌面与进程边界 | 待做 | Windows 长路径/Job Object 与 Linux 全流程 E2E 有证据 |

## 4. 已解决并纳入回归的体验问题

| 项目 | 状态 | 回归要求 |
|---|---|---|
| 启动迁移校验崩溃 | 已验证 | 已发布 migration 字节不可变测试 |
| macOS Keychain 无限等待 | 已验证 | 限时失败、缓存密钥、临时数据库不访问 Keychain |
| 弹窗跑到窗口底部 | 已验证 | 弹窗使用 body portal，水平和垂直居中 |
| 输入框每次只能输入一个字符 | 已验证 | Dialog 回调不因父组件重渲染重复抢焦点 |
| 底部操作按钮拥挤 | 已验证 | 风险和操作组靠右、允许安全换行 |
| 页面滚动后底部/右侧露白 | 已验证 | body/root 禁止滚动，仅业务面板拥有滚动；有 CSS 回归测试 |
| 历史 revision 无清单仍可点击复验 | 已验证 | 无 manifest 时禁用并给出原因 |
| 历史危险提交可绕过新保护批准 | 已验证 | 批准前重新校验完整 commit range |

## 5. 每个功能包必须包含的内容

每个 `06.xx-*.md` 至少包含：

1. 用户问题与风险；
2. 当前代码和后端契约；
3. 用户流程；
4. 数据字段与错误状态；
5. 页面和组件改动；
6. 不允许发生的行为；
7. 单元测试、集成测试和原生 E2E；
8. 完成证据；
9. 遗留问题。

## 6. 单项交付门禁

每完成一个功能包必须依次执行：

1. 相关 Rust 单元/集成测试；
2. 相关 Bun 测试；
3. `bun run typecheck`；
4. `cargo fmt --all -- --check`；
5. `cargo clippy --workspace --all-targets --release -- -D warnings`（涉及 Rust 时）；
6. 原生 AgentFlow.app 对应场景实测；
7. 更新功能包文档的测试证据和状态。

任何一步失败，都先修复当前功能包，不并行进入下一项。

## 7. 整体发布门禁

全部功能包合并后至少执行：

- `bun test src`
- `bun run typecheck`
- `cargo test --workspace --release`
- `cargo clippy --workspace --all-targets --release -- -D warnings`
- 签名构建 macOS `.app` 和 `.dmg`
- 启动、任务列表、任务详情五个标签、设置页完整冒烟测试
- daemon 关闭 UI 后继续运行、重开 UI 后恢复状态
- 无数据损坏、无未经确认的外发、无危险操作自动执行

## 8. Claude 实施约束

- 一次只接收一个功能包文档，不同时修改多个功能包。
- 不直接编辑生成的 `src/generated/bindings.ts`。
- 契约变化先改 Rust 类型和 Command，再重新生成绑定。
- 单个手写 TS/TSX/Rust 文件尽量不超过 600 行。
- 默认界面只展示用户需要的信息，技术字段放展开区。
- 危险操作必须先预览影响，并要求明确确认。
- 失败必须说明“发生了什么、数据是否安全、下一步怎么做”。
- 新增状态必须有加载、空、成功、失败、过期五类表现。
- 新增交互必须有键盘焦点、禁用原因和可访问名称。

## 9. 当前真实结论

- 后端核心工作流和治理能力已有较强自动化覆盖。
- 桌面端基础入口较完整，但部分功能仍只有“能操作”的第一层界面，缺少预检、进度、差异和失败恢复闭环。
- 当前不能宣称“所有桌面功能均完成真实 E2E”；必须以本清单逐项转为“已验证”。
