# DEX-08 审计事件导出入口 v1.0

> 状态：功能底座已实现，待 Claude 体验完善与原生验收  
> 优先级：P1

## 1. 用户问题与风险

旧版只有一个未展示在桌面的 `events_export(project_id)` 命令，返回文件路径但没有正式入口、范围选择、数量摘要或脱敏说明。更严重的是旧查询会把所有 `task_id IS NULL` 的全局事件混入任意项目导出，原始 `payload_json` 也可能携带 token、密码或授权头。

用户需要的不是一堆内部 JSON，而是一个能回答以下问题的清晰操作：

- 导出的是整个项目还是一个任务；
- 共包含多少事件和任务；
- 文件保存在哪里、大小是多少；
- 是否已经脱敏；
- 是否混入了其他项目的数据。

## 2. 已实现的安全协议

新增 `AuditExportResult`：

- `scope`：`project` 或 `task`；
- `projectId`、可选 `taskId`；
- `eventCount`、`taskCount`、`bytes`；
- `redacted`、`createdAt`；
- 本地绝对 `path`。

导出的 JSONL 第一行是 `agentflow.audit_export` 元数据，其余每行才是一个 `event`。格式带 `schema_version=1`，方便未来工具稳定解析。

底层执行以下边界保护：

1. 任务范围必须属于指定项目，否则返回 `AUDIT_EXPORT_TASK_OUTSIDE_PROJECT`；
2. 项目范围只包含该项目任务事件；
3. 无 `task_id` 的事件只有 payload 明确含当前 `project_id` 时才包含；
4. API key、token、password、secret、authorization、credential、private key 等字段递归替换为 `[REDACTED]`；
5. 字符串中的常见 `sk-*`、GitHub token、AWS key、Bearer Header 和密码模式再次脱敏；
6. 先写唯一临时文件、flush 和 sync，最后原子 rename，避免半截导出文件；
7. 导出动作自身写入审计事件，但不记录导出文件内容或秘密。

## 3. 已实现的桌面功能底座

- 项目任务列表右上角新增“导出审计”入口；
- 弹窗可选择整个项目或一个具体任务；
- 明确说明本地 JSONL、秘密字段脱敏和跨项目隔离；
- 导出成功后展示事件数、任务数、文件大小和完整保存位置；
- 保存路径可复制，也可以手动选择文本；
- 重复导出会生成新文件，不覆盖旧文件；
- 导出失败保留弹窗和范围选择，并显示错误。

## 4. Claude 前端完善清单

- [ ] 保留项目页的正式入口；如设置页已有“数据与存储”区，可增加同一入口，但不得实现第二套导出逻辑；
- [ ] 默认选择“整个项目”，任务列表使用 `TASK-xxx + 标题`，长标题截断但 tooltip 可查看；
- [ ] 弹窗第一屏只显示范围和安全说明，不展示原始 JSON；
- [ ] 脱敏状态不能只用绿色，必须同时有“已脱敏”文字和图标；
- [ ] 导出过程中禁止重复点击，显示明确进度状态；
- [ ] 导出成功卡片突出保存位置，提供“复制路径”；若新增“在 Finder 中显示”，必须调用受控 Tauri Command，不能拼 shell 字符串；
- [ ] 路径复制失败时保留可选择文本，不让用户丢失文件位置；
- [ ] 项目没有任务时仍允许项目级导出；
- [ ] 任务已删除但仍有审计记录时，项目导出不得因 UI 没有该任务而失败；
- [ ] 错误文案说明导出是否生成文件，不能只显示技术错误码；
- [ ] 200% 缩放、长中文标题、超长本地路径下弹窗按钮始终可达；
- [ ] 使用键盘完成范围选择、导出、复制和关闭，焦点关闭后回到触发按钮；
- [ ] 不直接编辑 `src/generated/bindings.ts`，使用当前 Rust 生成契约；
- [ ] 不把整个 JSONL 内容加载到桌面界面，也不自动上传或外发导出文件。

## 5. 测试证据

| 测试 | 结果 | 证据 |
|---|---|---|
| 项目/任务范围隔离 | 通过 | Rust 集成测试覆盖另一个项目与项目级事件 |
| 递归秘密脱敏 | 通过 | key 字段、嵌套授权头、字符串 token、密码 |
| 原子写入 | 通过 | 完成后不存在 `.tmp-*` 残留 |
| 跨项目任务故障注入 | 通过 | 错误码 `AUDIT_EXPORT_TASK_OUTSIDE_PROJECT` |
| 前端成功摘要 SSR | 通过 | 事件数、任务数、大小、保存路径 |
| TypeScript | 通过 | `tsc --noEmit` |
| 桌面构建 | 通过 | `.app` 与 `.dmg` 已生成并完成 ad-hoc 签名验证 |
| 原生 AgentFlow.app | 待执行 | 当前 macOS 锁屏；解锁安装后执行第 6 节 |

## 6. 原生 E2E 验收

1. 在包含多个任务的测试项目打开任务列表；
2. 点击“导出审计”，先导出整个项目；
3. 核对成功摘要的事件数、任务数、文件大小和保存位置；
4. 复制路径并打开文件，确认第一行是 schema 元数据；
5. 搜索测试 token，确认原值不存在且出现 `[REDACTED]`；
6. 再选择一个任务导出，确认不存在其他任务和项目级事件；
7. 快速连续点击，确认只执行一次；
8. 使用键盘和 200% 缩放重复流程；
9. 删除测试导出文件，确认应用运行和审计数据库不受影响。

## 7. 不允许发生的行为

- 不允许导出其他项目的任务或无归属全局事件；
- 不允许把原始凭据、授权头或密码写入导出文件；
- 不允许未完成写入的临时文件被当成成功结果；
- 不允许默认上传、分享或打开网络连接；
- 不允许用“导出成功”替代真实路径和计数；
- 不允许桌面端自行扫描数据库或绕过 Tauri Command。

## 8. 完成定义

- 第 5 节自动化测试全部通过；
- Claude 第 4 节全部勾选；
- 原生 E2E 全部通过；
- 导出文件可删除且不影响 AgentFlow 数据库；
- 总清单 DEX-08 标记为“已验证”。
